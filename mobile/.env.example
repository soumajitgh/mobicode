# Must exactly match PUBLIC_BASE_URL for the server being tested.
EXPO_PUBLIC_API_BASE_URL=http://localhost:8080

# Must be the same development-only nsec as DEV_NSEC in the server's .env.
# Never use a personal or production Nostr key here.
EXPO_PUBLIC_DEV_NSEC=
