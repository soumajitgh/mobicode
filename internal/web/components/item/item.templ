package item

import "github.com/soumajitgh/mobicode/internal/web/utils"

type Variant string
type Size string

const (
	VariantDefault Variant = "default"
	VariantOutline Variant = "outline"
	VariantMuted   Variant = "muted"
)

const (
	SizeDefault Size = "default"
	SizeSm      Size = "sm"
	SizeXs      Size = "xs"
)

type MediaVariant string

const (
	MediaVariantDefault MediaVariant = "default"
	MediaVariantIcon    MediaVariant = "icon"
	MediaVariantImage   MediaVariant = "image"
)

type Props struct {
	ID         string
	Class      string
	Attributes templ.Attributes
	Variant    Variant
	Size       Size
	Href       string
	Target     string
}

type GroupProps struct {
	ID         string
	Class      string
	Attributes templ.Attributes
}

type MediaProps struct {
	ID         string
	Class      string
	Attributes templ.Attributes
	Variant    MediaVariant
}

type ContentProps struct {
	ID         string
	Class      string
	Attributes templ.Attributes
}

type TitleProps struct {
	ID         string
	Class      string
	Attributes templ.Attributes
}

type DescriptionProps struct {
	ID         string
	Class      string
	Attributes templ.Attributes
}

type ActionsProps struct {
	ID         string
	Class      string
	Attributes templ.Attributes
}

type HeaderProps struct {
	ID         string
	Class      string
	Attributes templ.Attributes
}

type FooterProps struct {
	ID         string
	Class      string
	Attributes templ.Attributes
}

type SeparatorProps struct {
	ID         string
	Class      string
	Attributes templ.Attributes
}

// 1:1 base/ui/item.tsx: structure lives here, the look comes from the
// active style-*.css via the classes.
func itemClasses(variant Variant, size Size) string {
	base := "[a]:hover:bg-muted rounded-lg border text-sm group/item flex w-full flex-wrap items-center transition-colors duration-100 outline-none focus-visible:border-ring focus-visible:ring-[3px] focus-visible:ring-ring/50 [a]:transition-colors"
	return utils.CN(base, variantClasses(variant), sizeClasses(size))
}

func variantClasses(variant Variant) string {
	switch variant {
	case VariantOutline:
		return "border-border"
	case VariantMuted:
		return "bg-muted/50 border-transparent"
	default:
		return "border-transparent"
	}
}

func sizeClasses(size Size) string {
	switch size {
	case SizeSm:
		return "gap-2.5 px-3 py-2.5"
	case SizeXs:
		return "gap-2 px-2.5 py-2 in-data-[slot=dropdown-menu-content]:p-0"
	default:
		return "gap-2.5 px-3 py-2.5"
	}
}

func mediaClasses(variant MediaVariant) string {
	switch variant {
	case MediaVariantIcon:
		return "[&_svg:not([class*='size-'])]:size-4"
	case MediaVariantImage:
		return "size-10 overflow-hidden rounded-sm group-data-[size=sm]/item:size-8 group-data-[size=xs]/item:size-6 [&_img]:size-full [&_img]:object-cover"
	default:
		return "bg-transparent"
	}
}

templ Group(props ...GroupProps) {
	{{ var p GroupProps }}
	if len(props) > 0 {
		{{ p = props[0] }}
	}
	{{ class := "gap-4 has-data-[size=sm]:gap-2.5 has-data-[size=xs]:gap-2 group/item-group flex w-full flex-col" }}
	{{ class = utils.CN(class, p.Class) }}
	<div
		if p.ID != "" {
			id={ p.ID }
		}
		role="list"
		data-slot="item-group"
		class={ class }
		{ p.Attributes... }
	>
		{ children... }
	</div>
}

templ Item(props ...Props) {
	{{ var p Props }}
	if len(props) > 0 {
		{{ p = props[0] }}
	}
	if p.Variant == "" {
		{{ p.Variant = VariantDefault }}
	}
	if p.Size == "" {
		{{ p.Size = SizeDefault }}
	}
	if p.Href != "" {
		<a
			if p.ID != "" {
				id={ p.ID }
			}
			href={ templ.SafeURL(p.Href) }
			if p.Target != "" {
				target={ p.Target }
			}
			data-slot="item"
			data-variant={ string(p.Variant) }
			data-size={ string(p.Size) }
			class={ utils.CN(itemClasses(p.Variant, p.Size), p.Class) }
			{ p.Attributes... }
		>
			{ children... }
		</a>
	} else {
		<div
			if p.ID != "" {
				id={ p.ID }
			}
			data-slot="item"
			data-variant={ string(p.Variant) }
			data-size={ string(p.Size) }
			class={ utils.CN(itemClasses(p.Variant, p.Size), p.Class) }
			{ p.Attributes... }
		>
			{ children... }
		</div>
	}
}

templ Media(props ...MediaProps) {
	{{ var p MediaProps }}
	if len(props) > 0 {
		{{ p = props[0] }}
	}
	if p.Variant == "" {
		{{ p.Variant = MediaVariantDefault }}
	}
	{{ class := "gap-2 group-has-data-[slot=item-description]/item:translate-y-0.5 group-has-data-[slot=item-description]/item:self-start flex shrink-0 items-center justify-center [&_svg]:pointer-events-none" }}
	{{ class = utils.CN(class, mediaClasses(p.Variant), p.Class) }}
	<div
		if p.ID != "" {
			id={ p.ID }
		}
		data-slot="item-media"
		data-variant={ string(p.Variant) }
		class={ class }
		{ p.Attributes... }
	>
		{ children... }
	</div>
}

templ Content(props ...ContentProps) {
	{{ var p ContentProps }}
	if len(props) > 0 {
		{{ p = props[0] }}
	}
	{{ class := "gap-1 group-data-[size=xs]/item:gap-0 flex flex-1 flex-col [&+[data-slot=item-content]]:flex-none" }}
	{{ class = utils.CN(class, p.Class) }}
	<div
		if p.ID != "" {
			id={ p.ID }
		}
		data-slot="item-content"
		class={ class }
		{ p.Attributes... }
	>
		{ children... }
	</div>
}

templ Title(props ...TitleProps) {
	{{ var p TitleProps }}
	if len(props) > 0 {
		{{ p = props[0] }}
	}
	<div
		if p.ID != "" {
			id={ p.ID }
		}
		data-slot="item-title"
		class={ utils.CN("gap-2 text-sm leading-snug font-medium underline-offset-4 line-clamp-1 flex w-fit items-center", p.Class) }
		{ p.Attributes... }
	>
		{ children... }
	</div>
}

templ Description(props ...DescriptionProps) {
	{{ var p DescriptionProps }}
	if len(props) > 0 {
		{{ p = props[0] }}
	}
	{{ class := "text-muted-foreground text-left text-sm leading-normal group-data-[size=xs]/item:text-xs line-clamp-2 font-normal [&>a]:underline [&>a]:underline-offset-4 [&>a:hover]:text-primary" }}
	{{ class = utils.CN(class, p.Class) }}
	<p
		if p.ID != "" {
			id={ p.ID }
		}
		data-slot="item-description"
		class={ class }
		{ p.Attributes... }
	>
		{ children... }
	</p>
}

templ Actions(props ...ActionsProps) {
	{{ var p ActionsProps }}
	if len(props) > 0 {
		{{ p = props[0] }}
	}
	<div
		if p.ID != "" {
			id={ p.ID }
		}
		data-slot="item-actions"
		class={ utils.CN("gap-2 flex items-center", p.Class) }
		{ p.Attributes... }
	>
		{ children... }
	</div>
}

templ Header(props ...HeaderProps) {
	{{ var p HeaderProps }}
	if len(props) > 0 {
		{{ p = props[0] }}
	}
	<div
		if p.ID != "" {
			id={ p.ID }
		}
		data-slot="item-header"
		class={ utils.CN("gap-2 flex basis-full items-center justify-between", p.Class) }
		{ p.Attributes... }
	>
		{ children... }
	</div>
}

templ Footer(props ...FooterProps) {
	{{ var p FooterProps }}
	if len(props) > 0 {
		{{ p = props[0] }}
	}
	<div
		if p.ID != "" {
			id={ p.ID }
		}
		data-slot="item-footer"
		class={ utils.CN("gap-2 flex basis-full items-center justify-between", p.Class) }
		{ p.Attributes... }
	>
		{ children... }
	</div>
}

templ Separator(props ...SeparatorProps) {
	{{ var p SeparatorProps }}
	if len(props) > 0 {
		{{ p = props[0] }}
	}
	<div
		if p.ID != "" {
			id={ p.ID }
		}
		data-slot="item-separator"
		data-orientation="horizontal"
		role="separator"
		aria-orientation="horizontal"
		class={ utils.CN("shrink-0 bg-border data-horizontal:h-px data-horizontal:w-full data-vertical:w-px data-vertical:self-stretch", "my-2", p.Class) }
		{ p.Attributes... }
	></div>
}
