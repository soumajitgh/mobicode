root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o ./tmp/server ./cmd/api"
  bin = "./tmp/server"
  delay = 1000
  exclude_dir = ["data", "docs", "tmp", "vendor"]
  include_ext = ["go"]
  stop_on_error = true

[log]
  time = true

[misc]
  clean_on_exit = true
