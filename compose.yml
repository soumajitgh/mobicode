services:
  libsql:
    image: ghcr.io/tursodatabase/libsql-server:latest
    platform: linux/amd64
    ports:
      - "${LIBSQL_PORT:-9081}:8080"
    environment:
      SQLD_NODE: primary
      SQLD_DB_PATH: mobicode.db
    volumes:
      - libsql-data:/var/lib/sqld

volumes:
  libsql-data:
